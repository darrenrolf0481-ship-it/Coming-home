1. **Explore `index.tsx`**: Read `index.tsx` to find the `mousemove` event listeners.
2. **Implement Throttle**: Modify the `resetIdle` function inside `useEffect` in `index.tsx` to throttle executions to at most once per 200ms using `Date.now()`.
3. **Run tests**: Ensure `npm test` and `npm run build` pass.
4. **Pre-commit**: Complete pre-commit steps to make sure proper testing, verifications, reviews and reflections are done.
5. **Submit**: Create PR with a title "⚡ Bolt: [performance improvement]" and required description.
